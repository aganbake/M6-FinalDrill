// Modules
const express = require("express");

// Requires
const app = express();
const PORT = 3000;

// CRUD Operations

// Routes

// Server
app.listen(PORT, () => `Server Up and running on port ${PORT}`);
